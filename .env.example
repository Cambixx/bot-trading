# Ejemplo de variables de entorno
# Copiar este archivo a .env y configurar con tus propios valores

# ⚠️ IMPORTANTE: Nunca subir .env al repositorio (está en .gitignore)

# API Key de Gemini (obtener de https://aistudio.google.com/app/apikey)
# Para desarrollo local (frontend)
VITE_GEMINI_API_KEY=tu_nueva_api_key_aqui

# Para producción en Netlify (serverless function)
# Esta variable se configura en Netlify Dashboard, no aquí
GEMINI_API_KEY=tu_nueva_api_key_aqui
